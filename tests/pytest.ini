[pytest]
addopts = -v --tb=short --strict-markers
testpaths = tests
markers =
    unit:        no containers needed
    integration: requires docker-compose stack up
    e2e:         long-running scenarios (mass provisioning, etc.)
    slow:        runs > 30s
filterwarnings =
    ignore::DeprecationWarning
